@page
@model BookProgram.Pages.Koordinator.Fags.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>All Courses In The System</h1>

<p>
    <a asp-page="Create" class="btn btn-outline-success">Create New</a>
</p>
<hr style="width: auto" , size="10" , color=black>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Fag[0].FagName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Fag) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FagName)
            </td>
            <td>
                    <a asp-page="./Edit" asp-route-id="@item.ID" class="btn btn-outline-primary">Edit</a> |

                        <a asp-page="./Delete" asp-route-id="@item.ID" class="btn btn-outline-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
